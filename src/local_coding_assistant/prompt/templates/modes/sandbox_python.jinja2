{% set ctx = context %}
PROGRAMMATIC TOOL CALLING (Python Sandbox)
Guidance:
1. If you call a tool, YOU MUST do it through code argument of EXECUTE_PYTHON_CODE tool.
2. Any direct tool call is forbidden.
3. Import allowed helpers from tools_api and call them directly.
4. Print intermediate diagnostics when useful, all stdout will be captured and returned; always finish by calling final_answer(...).
5. Keep programs deterministic and idempotent; re-run safety matters.
