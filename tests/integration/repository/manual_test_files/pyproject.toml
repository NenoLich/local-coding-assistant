[build-system]
requires = ["uv_build>=0.8.23,<0.9.0"]
build-backend = "uv_build"

[project]
name = "local-coding-assistant"
version = "0.1.0"
description = "An AI-powered local coding assistant with plugin support"
readme = "README.md"
requires-python = ">=3.12"
authors = [
    { name = "NenoLich", email = "ne0fun907@gmail.com" },
]
license = { text = "MIT" }
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3.12",
    "Environment :: Console",
    "Topic :: Software Development",
    "Typing :: Typed",
]
dependencies = [
    "pydantic>=2.12.0",
    "typer>=0.19.2",
    "python-dotenv>=1.0.0",
    "langgraph>=1.0.0a4",
    "docker>=7.1.0",
    "docstring-parser>=0.17.0",
    "jinja2>=3.1.4",
    "structlog>=25.5.0",
    "rich>=14.2.0",
    "uvicorn[standard]>=0.25.0",
    "fastapi[all]>=0.135.1",
    "psutil>=6.0.0",
    "websockets>=16.0",
    "httpx>=0.28.1",
    "litellm>=1.94.0",
    "tree-sitter>=0.26.0",
    "tree-sitter-python>=0.25.0",
    "tree-sitter-language-pack>=1.13.7",
    "watchdog>=6.0.0",
    "orjson>=3.11.4",
    "networkx>=3.6.1",
    "numpy>=2.0.0",
    "scipy>=1.18.0",
]

[project.scripts]
locca = "local_coding_assistant.cli.main:main"

[tool.taskipy.tasks]
test-unit = "pytest -v -s -x tests/unit"
test-integration = "pytest -v -s -x tests/integration"
test-e2e = "pytest -v -s -x tests/e2e"
test-all = "pytest -v -s -x tests"

[tool.ruff.lint]
select = [
    "E", "F", "I", "B", "A",  # Base rules
    "UP", "N", "Q", "RUF",    # Additional rules
    "C90", "S", "T20", "PT",  # Complexity, security, print statements, pytest
]
ignore = [
    "E501",  # Line length (handled by ruff format)
    "S101",  # Allow `assert` in tests
    "T201",  # Allow `print` in tests
]
exclude = ["**/sandbox/guest/tools_api.py"]

[tool.ruff.lint.per-file-ignores]
"tests/**" = ["S101", "T201"]  # Ignore assert/print in tests

[tool.ruff.lint.mccabe]
max-complexity = 10

[tool.ruff.lint.isort]
known-first-party = ["local_coding_assistant"]

[tool.ty.rules]
unused-ignore-comment = "warn"
redundant-cast = "ignore"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
pythonpath = ["src"]
asyncio_mode = "auto"
# addopts = "-v --cov=local_coding_assistant --cov-report=term-missing"
filterwarnings = ["ignore::DeprecationWarning"]
enable_assertion_pass_hook = true

[coverage.run]
source = ["src"]
omit = ["tests/*"]

[coverage.report]
show_missing = true
skip_covered = true

[dependency-groups]
# Modern PEP 735 format - recommended for new projects
dev = [
    "pytest>=8.0.0",
    "pytest-cov>=4.0.0",
    "pytest-mock>=3.12.0",
    "ruff>=0.6.0",
    "ty>=0.0.1a20",
    "taskipy>=1.14.1",
    "pyupgrade>=3.21.0",
    "pytest-asyncio>=1.3.0",
    "pre-commit>=4.5.0",
    "pytest-golden>=1.0.1",
    "mkdocs>=1.5.0",
    "mkdocs-material>=9.0.0",
    "mkdocstrings-python>=2.0.3",
    "mkdocs-mermaid2-plugin>=1.1.1",
]
