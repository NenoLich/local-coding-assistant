# Production Environment Configuration
# Copy this file to .env.production and adjust as needed
# WARNING: This file should NEVER be committed to version control

# Core Settings
LOCCA_ENV=production
LOCCA_DEBUG=false
LOCCA_LOG_LEVEL=INFO
LOCCA_DEV_MODE=false

# Paths (will be expanded with user home directory)
LOCCA_CONFIG_DIR=~/.local-coding-assistant
LOCCA_CACHE_DIR=~/.cache/local-coding-assistant

# Security (set these in your production environment)
# SECRET_KEY=your-secret-key-here
# DATABASE_URL=postgresql://user:password@localhost/production_db

# Production API keys (set these in your production environment)
# OPENAI_API_KEY=your-production-api-key-here

# Rate limiting (adjust as needed)
RATE_LIMIT_ENABLED=true
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_PERIOD=3600  # 1 hour

# CORS settings (adjust for your deployment)
# CORS_ORIGINS=https://yourdomain.com,https://app.yourdomain.com
